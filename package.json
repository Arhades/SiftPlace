{
  "name": "siftplace",
  "private": true,
  "version": "0.0.0",
  "description": "Run the SiftPlace backend (FastAPI) and app (Vite) together with one command.",
  "scripts": {
    "dev": "concurrently -k -n backend,app -c yellow,cyan \"npm:dev:backend\" \"npm:dev:app\"",
    "dev:backend": "backend\\.venv\\Scripts\\python.exe -m uvicorn main:app --reload --reload-dir backend --port 8000 --app-dir backend",
    "dev:app": "npm --prefix app run dev",
    "setup": "python -m venv backend\\.venv && backend\\.venv\\Scripts\\python.exe -m pip install -r backend\\requirements.txt && npm --prefix app install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
